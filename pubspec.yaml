name: caixinha
description: Aplicativo de orçamento pessoal

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.0
  fl_chart: ^0.65.0
  shared_preferences: ^2.2.1
  cupertino_icons: ^1.0.2
  url_launcher: ^6.1.10
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - lib/images/finance_illustration.jpg

